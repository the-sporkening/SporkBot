
/*async function makeChannel(server, name, cat, type){
    // Create a new category channel with permission overwrites
    await server.createChannel(name, type)
        .then(console.log)
        .catch(console.error);
    await server.channels.find("name", args[0]).setParent('535881919108218902').catch(console.error);
}*/